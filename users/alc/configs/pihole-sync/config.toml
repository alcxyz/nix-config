# Pi-hole Sync Configuration

[primary]
host = "192.168.1.15"
port = 8081

[secondary]
host = "192.168.1.3"
port = 8081

[sync]
# Sync interval (for reference, cron handles the actual scheduling)
interval_minutes = 60

[logging]
file = "./logs/pihole-sync.log"
